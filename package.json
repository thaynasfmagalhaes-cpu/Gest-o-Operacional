{
  "name": "equipa-gestao-equipamentos",
  "version": "2.0.0",
  "private": true,
  "description": "Equipa — plataforma de gestão e rastreabilidade de equipamentos",
  "type": "module",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker.js && node --check public/app.js && node --check public/ux-agent.js && node --check public/microinteractions.js"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
