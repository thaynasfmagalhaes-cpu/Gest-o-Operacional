{
  "name": "gestao-operacional-cloudflare",
  "version": "2.0.0",
  "private": true,
  "description": "Gestão operacional de equipamentos com assistente UI/UX",
  "type": "module",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check worker.js && node --check public/app.js && node --check public/ux-agent.js"
  },
  "devDependencies": {
    "wrangler": "^4.38.0"
  }
}
